# -*- coding:utf-8 -*-

name = input("Please input your name: ")
print("Hello " + name + '.')

prompt = "If you tell us who you are, we can pensonalize the messages you see."
prompt += "\nWhat's your first name?"
first_name = input(prompt)
print(first_name)